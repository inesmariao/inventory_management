from django.urls import path, include

urlpatterns = [
    # Future v2 endpoints
    # path('identity/', include('apps.identity.api.v2.urls')),
    # path('company/', include('apps.company.api.v2.urls')),
]
